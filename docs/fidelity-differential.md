# Fidelity spot-check: arm A vs aider's real extractor

Generated by `scripts/fidelity_differential.py` running **aider's actual `RepoMap.get_tags`** (Apache-2.0) against this project's arm-A parse on the same django files. Two files; a spot-check, not a study.

**Scope limit, stated first:** aider builds a PageRank-ranked identifier map from tree-sitter def/ref tags — it has **no call-edge set**, so edge-level fidelity is not claimable. What is comparable is the def-symbol set per file: does arm A's parse see the definitions aider's parse sees?

| File | aider defs | seen by arm A | coverage |
|---|---|---|---|
| `django/db/models/expressions.py` | 92 | 92 | 100.00% |
| `django/core/validators.py` | 27 | 27 | 100.00% |

Symbols aider tags that arm A does not (first 10 per file):

- `django/db/models/expressions.py`: (none)
- `django/core/validators.py`: (none)

Raw artifact: `data/shipped/fidelity.json`. The claim this supports is narrow and stated as such: arm A is built from the same raw def material deployed name-matching tools extract. The edge construction on top of it is this project's own, characterised against type resolution in `docs/graph-delta.md`.
