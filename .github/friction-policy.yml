# substrate-friction repo policy — maintainer-set, disclosed in every comment.
# The demo unlock (PR #4) used threshold: 0.30; the shipped default is
# restored here. Set your own bar consciously: the bar is policy,
# the evidence is statistical (docs/objections.md §8).
threshold: 0.95
